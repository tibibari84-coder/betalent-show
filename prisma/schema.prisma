// BeTalent — database schema will grow in later phases. Intentionally empty of models for now.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
